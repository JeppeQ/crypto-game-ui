service: client

runtime: nodejs12

handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
    secure: always

  - url: /static
    static_dir: build/static

env_variables:
  NODE_ENV: 'production'