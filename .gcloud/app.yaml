service: client

runtime: nodejs12

handlers:
  - url: /
    static_files: dist/index.html
    upload: dist/index.html
    secure: always

  - url: /
    static_dir: dist

env_variables:
  NODE_ENV: 'production'